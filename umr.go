package xmux

// url资源的path,

type UMR map[string]MethodsRoute

// func (u UMR) add() {
// 	for method := range u {
// 		if _, ok := u; ok {
// 			if _, gok := u[method]; gok {
// 				log.Fatal("method : " + method + "  duplicate, url: " + url)
// 			}
// 		}
// 		g.merge(group, group.tpl[url][method])
// 	}
// 	u = group.tpl[url]

// }
